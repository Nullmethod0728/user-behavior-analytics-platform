# ============================================================
# 用户行为日志生成器 - Python 依赖
# 安装方式: pip install -r requirements.txt
# ============================================================

# Kafka 客户端 — 把日志发送到 Kafka
kafka-python>=2.0.2

# 假数据生成器 — 生成随机用户、城市、IP、设备型号等
faker>=22.0.0

# 命令行参数解析 — 支持 --rate --duration 等参数
click>=8.1.0

# 进度条 — 终端显示生成进度
tqdm>=4.66.0
