-- ============================================================
-- DWD 层 (Data Warehouse Detail) — 明细数据层
-- 职责：清洗后的明细数据，去重、校验、补全
-- 数据来源：从 ODS 清洗而来 (Spark OdsToDwd)
-- 保留时间：90 天
-- ============================================================

-- 表名: analytics.dwd_user_behavior
-- 引擎: ReplacingMergeTree()  ← 支持幂等写入
-- 字段与 ODS 完全相同，但数据质量更高
